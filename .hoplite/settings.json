{
	"version": 1,
	"ports": {
		"preview": 3000,
		"additional": {}
	},
	"scripts": {
		"setup": {
			"enabled": true,
			"command": "command -v mise >/dev/null 2>&1 || { curl -fsSL https://mise.run | bash; export PATH=\"$HOME/.local/bin:$PATH\"; }; mise install && mise exec -- go version"
		},
		"run": {
			"enabled": true,
			"command": "cd tui-preview && mise exec -- bash -c 'npm install --no-audit --no-fund && npm run dev'"
		},
		"archive": {
			"enabled": true,
			"command": null
		},
		"check": {
			"enabled": true,
			"command": null
		}
	},
	"mcpServers": []
}
